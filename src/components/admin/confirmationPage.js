import React from 'react'

const confirmationPage = ({ deleteHandler }) => {
    return (
        <>
            const check = window.confirm('do u want to updatae this :');

            if (check) {
                setVisible(true);
            setUpdatedName(c.name);
            setSelected(c)
        }
            else {
                setVisible(false);
            setUpdatedName("");
            setSelected(null);
        }
        </>
    )
}

export default confirmationPage